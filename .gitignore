# --- Credentials / secrets ---
credentials.json
*credentials*.json
.env
.env.*
!.env.example

# --- Local data (NEVER commit DICOMs or PHI-bearing manifests) ---
data/
datasets/
raw/
processed/

# --- Medical image formats ---
*.dcm
*.dicom
*.nii
*.nii.gz
*.mha
*.nrrd

# --- MIDRC / Gen3 manifests and tokens (may contain object IDs / identifiers) ---
*.manifest.json
manifest*.json
gen3-*.log
.gen3/

# --- Outputs that may include identifiers ---
# Keep small aggregated summary CSVs in outputs/ committable by default,
# but ignore obvious metadata dumps that include PatientID etc.
outputs/dicom_file_metadata.csv

# --- Local model training artifacts ---
models/
checkpoints/
outputs/resnet18_head_chest/
*.pt
*.pth

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
.venv311/
venv/
.ipynb_checkpoints/

# --- Local tools ---
bin/gen3-client

# --- OS ---
.DS_Store
Thumbs.db
